# API base URL (no trailing slash).
# Local: backend at http://localhost:8000 with routes /order, /logistics, /payment, /audit, /user
# Production: set to your backend URL (e.g. https://api.example.com)
VITE_API_BASE_URL=http://localhost:8000
